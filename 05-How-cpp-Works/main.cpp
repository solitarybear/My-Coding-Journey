#include <iostream>

int main() //here the return type of main function is integer but in main functio if don't write anything it return 0
{
    std::cout <<"Hello World" << std::endl; //<< these are operator, u can think them as function 
    std::cin.get(); //here the function get will wait for us to press enter and after then our execution of code completed
}