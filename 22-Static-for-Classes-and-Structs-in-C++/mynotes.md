#  static methods cannot access non-static variables

every method that you write inside a class 

every non-static method always gets an instance of the current class as a parameter that's how classes actually work behind the scenes 

there's no such thing as a class they just functions with a hidden parameter of sorts 

a static method does not get that hidden parameter a static method is the same as if you wrote a method outside of the class 