# Smartpointers 
u not delete manually if created memory on heap and in some type of smartpointer also not need to use both new and delete keyword  

# unique pointers 
unique pointers are one of the simple kind of smart pointer and unique pointers are scoped pointers

you can't copy a unique pointer 

unique pointers are when u need a scoped pointer    

# std::unique_ptr <Entity> entity (new Entity());
std::___(name of pointer)__ <Template parameter> name_of_object (intialization);

#  